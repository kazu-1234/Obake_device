# Obake_device 引き継ぎ書

別チャット・別 PC でこの計画を続けるための正本。  
最終更新: 2026-09-09 / 表示版 **v0.5.1**

このフォルダ一式（`Obake_device`）をコピーする。Wi-Fi / API キー / OTA URL はリポに無い。  
Flash バックアップと戻し方: [バックアップと戻し方.md](バックアップと戻し方.md)

**いまの段階:** Arduino ハード確認は完了。**本番は Stack-chan ESP-IDF（会話＝当面 Xiaozhi）**。  
CUSTOM セッション向けにおばけ顔（口・両目・ToF）を custom/obake に移植済み。  
Arduino の `.ino` は本番経路ではない。**サーボ（標準首含む）は当面完全停止**。  
吹き出しは CUSTOM では出さない。下スワイプのホームは従来どおり。  
ウェイクは Multinet「おばけちゃん」（`sdkconfig.defaults.local`）。別 PC への会話転送は未実装。

---

## 1. 何を作るか（目的）

ハッカソン向けの **空中にいる白い丸いおばけロボット**。  
コンセプト図: [reference/content.png](reference/content.png)  
イメージ: ふわっとそばにいて、見守り・会話・軽い物渡し。呼びかけは **「おばけちゃん」**（今後変わる可能性あり）。

移動（天井ワイヤー等）のアクチュエータは **チームの別担当・別マイコン**。このリポ（Stack-chan CoreS3）は脳・顔・耳・口・距離・移動への指示。

---

## 2. 次段階の方針（v0.5.1）

ベースは **完全に Stack-chan**。おばけ専用の別メイン基板は置かない。

| 機能 | 方針 |
|------|------|
| 本体・SoC | Stack-chan の **CoreS3** |
| カメラ・マイク・スピーカ | **Stack-chan 内蔵のまま** |
| 会話スタック | **いったん Xiaozhi のまま**。差し替えやすい境界を残す |
| 呼びかけ | **「おばけちゃん」**（`obake_wake_config.h` で変更） |
| ウェイク時 LED | Stack-chan と同じでよい |
| CoreS3 液晶 | **口**（白地・黒の ∪／∩。Custom 時のみ） |
| 目 | PaHub CH0/CH1。**必須**。縦長黒楕円・閉は ∪／∩・SH1106 余列は白埋め |
| 床距離 | PaHub CH2 ToF。**必須**。Long Range。上限 **200 cm** |
| サーボ（足・首） | **当面完全停止**（`addon_servo_guard` の `kForceAllServosOff`） |
| 吹き出し | CUSTOM では非表示 |
| 下スワイプホーム | 公式のまま継続 |
| 呼びかけ | Multinet「おばけちゃん」（効き弱ければ AFE に戻す手順あり） |
| 移動 | 別マイコン。CoreS3 は指示だけ。通信は未定 |
| 別 PC 転送 | 未実装（将来。Stack-chan 側とは限らない） |
| 書き込み | **ESP-IDF**。Arduino は確認用のみ |

正本ファーム: `C:\Users\kazuh\Documents\Arduino\Stackchan\firmware`（IDF **v5.5.4**）。  
Obake 追加: `main/stackchan/custom/obake/`（`custom_integration` の hooks から起動）。

---

## 3. 要件定義

### 必須

| ID | 要件 | 現状の決め方 |
|----|------|----------------|
| R1 | 会話できる | 当面 Xiaozhi。差し替えやすく |
| R2 | 目 | SSD1306×2。描画まばたき。視覚は内蔵カメラ |
| R3 | 聞く・話す | Stack-chan マイク／スピーカ |
| R4 | 空中を移動できる | 別マイコン。CoreS3 は指示と距離 |
| R6 | 床距離 | ToF。上限 200 cm（CUSTOM で必須表示） |
| R8 | 呼びかけ | 「おばけちゃん」。LED は Stack-chan 流用 |
| R9 | 口の表示 | CoreS3 液晶に口（CUSTOM） |

### あるとよい / 移動まわり

| ID | 要件 | 現状の決め方 |
|----|------|----------------|
| R5 | 障害物 | カメラでだいたい |
| R7 | 目の開閉 | OLED 描画 |
| R10 | 「こっちに来て」 | 移動マイコンへコマンド |
| R11 | 距離を保つ | ToF で **20 cm 維持** 指示 |
| R12 | 移動通信 | 未定。スタブ先 |

### 制約

- はんだ・3Dプリンタなし
- 本番は ESP-IDF。確認用 `.ino` の前は必ず Flash バックアップ
- **当面サーボ回転なし**
- 秘密情報は git に出さない

---

## 4. 今どこまで出来ているか

### できている（ハード確認・Arduino）

- PaHub・両目・ToF・口 UI・まばたき・きょろきょろ・SH1106 端の白埋め
- Flash 16 MB バックアップ: `C:\Users\kazuh\Documents\stackchan_backup\stackchan_backup.bin`
- 確認用スケッチ: `arduino/obake_pahub_bringup/`（本番ではない）

### できている（本番 IDF・CUSTOM）

- `stackchan/custom/obake/` … Port.A 別 I2C、PaHub、両目、ToF、口 UI
- `IsCustomSessionActive()` のときだけ起動（通常 AI.AGENT は触らない）
- 呼びかけ: Multinet「おばけちゃん」（`sdkconfig` + `obake_wake_config.h`）。弱ければ AFE フォールバック可
- サーボ完全停止: `kForceAllServosOff`。CUSTOM で吹き出し非表示・下スワイプ維持

### これから

1. 実機 flash 後、CUSTOM で口・両目・距離を確認
2. Multinet「おばけちゃん」検出を試す（弱ければ AFE フォールバック）
3. 移動スタブ（サーボは触らない）
4. 会話独自化は後

### 版

[version_history.txt](version_history.txt)。表示 **v0.5.1**。

| 版 | 内容 |
|----|------|
| v0.1–v0.3.1 | 旧案 |
| v0.3.2–v0.4.x | 部品確定・Arduino 確認（目・ToF・口・サーボ試験） |
| v0.5.0 | Arduino 確認完了。本番は Stack-chan IDF。サーボ回転は当面なし |
| v0.5.1 | CUSTOM におばけ顔（口・両目・ToF）移植 |

---

## 5. アーキテクチャ

```
[人] 「おばけちゃん」
  → Stack-chan ウェイク（LED 現行のまま／モデルは当面 Hi StackChan）
  → マイク → Xiaozhi（後で差し替え）→ スピーカ
  → 液晶: 口（∪／∩）※CUSTOM
  → 目 OLED: 開＝縦長黒楕円／閉＝∪か∩ ※CUSTOM

[床] ToF → cm / 20 cm 維持用 ※CUSTOM

[移動] CoreS3 --(未定)--> 別マイコン
[サーボ] 当面オフ（回さない）
```

配線:

```
Stack-chan Port.A → PaHub v2.1 (0x70)
  CH0 左目 / CH1 右目 / CH2 ToF U010
```

IDF 側の要点:

- Port.A は **I2C port 0 / SDA=GPIO2 / SCL=GPIO1**（内蔵バス GPIO11/12・port1 と分離）
- SH1106: 列オフセット 2、**132 列を白で埋めてから** 128 列の絵
- ページごとに左右交互送信
- I2C ブロッキングは `obake_hw` タスク（LVGL lock 外）

---

## 6. ハードウェア

詳細は [README.md](README.md)。  
所持: Stack-chan、PaHub、SSD1306×2、ToF U010、Grove–Dupont。

---

## 7. 開発の始め方（本番）

1. USB 接続後、必要なら [バックアップと戻し方.md](バックアップと戻し方.md) で復元
2. 下の **ビルド／書き込み（手動）** で flash
3. ランチャーで **CUSTOM** を開く → 口・両目・距離
4. **AI.AGENT** は従来どおり（おばけ顔なし）

確認用参照: `arduino/obake_pahub_bringup/obake_pahub_bringup.ino`

### ビルド／書き込み（手動・PowerShell）

正本: `C:\Users\kazuh\Documents\Arduino\Stackchan\firmware`  
IDF: **v5.5.4**（EIM 導入済み想定）  
ポートはだいたい **COM3**（変わるので毎回確認）

**事前**

- CoreS3 本体下の USB-C（データ通信できるケーブル）
- Arduino IDE のシリアルモニタ／他の COM 占有ソフトは **閉じる**
- ポート確認例:

```powershell
[System.IO.Ports.SerialPort]::GetPortNames()
```

**IDF 環境を有効化してから build / flash**（毎回この順）

```powershell
$env:IDF_TOOLS_PATH = "C:\Espressif\tools"
. "C:\Espressif\tools\Microsoft.v5.5.4.PowerShell_profile.ps1"
Set-Location "C:\Users\kazuh\Documents\Arduino\Stackchan\firmware"

idf.py build
idf.py -p COM3 flash
```

- `export.ps1` 単体はパスが壊れやすい。**上の Microsoft profile** を使う
- 進捗を見るときは `| Select-Object -Last N` でパイプしない（バッファで止まったように見える）
- 書き込みだけ（前回ビルド済みのとき）: `idf.py -p COM3 flash`
- 失敗「Could not open COMx」: ケーブル抜き差し・シリアルモニタ閉鎖・ポート番号再確認。必要なら RST 約 3 秒長押しでダウンロードモード
- **assets サイズ:** Multinet 有効時 `build/generated_assets.bin` は約 5.5MB。`partitions.csv` の assets は **6MB**（不足するとランチャーアイコンが全部消える）。パーティション変更後は partition table ごと再 flash

**ウェイク（Multinet）**

- 有効時: `sdkconfig` で `CONFIG_USE_CUSTOM_WAKE_WORD=y`、AFE は off
- 文言: `CONFIG_CUSTOM_WAKE_WORD="o ba ke chan"` / 表示「おばけちゃん」
- `sdkconfig.defaults.local` は **新規 config 時**向き。既存 `sdkconfig` は手で直すか `idf.py menuconfig`
- 検出が弱いときは AFE（Hi StackChan）に戻し、表示名だけ `obake_wake_config.h` で「おばけちゃん」

**GUI**

- 16 MB バックアップ／戻し／bin 選択: [tools/obake_flash_gui.pyw](tools/obake_flash_gui.pyw)
- 詳細は [バックアップと戻し方.md](バックアップと戻し方.md)

---

## 8. 次にやること（優先順）

1. 実機 build / flash と CUSTOM 確認（口・両目・距離）
2. ウェイク Multinet 試し／表示名「おばけちゃん」
3. 移動スタブ
4. 会話独自化はあと

やらない（今）: サーボ回転、Catch、Arduino 本番化。

---

## 9. リポ内の地図

```
Obake_device/
  引き継ぎ.md
  バックアップと戻し方.md
  README.md
  version_history.txt
  arduino/obake_pahub_bringup/   確認用のみ
  firmware/                      旧骨格。正本は ../Stackchan/firmware
```

正本ファーム: `Documents\Arduino\Stackchan\firmware`  
おばけ実装: `main/stackchan/custom/obake/`

文字コード UTF-8。識別子は英語、説明は日本語。
