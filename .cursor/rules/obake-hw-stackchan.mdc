---
description: Obake は Stack-chan ベース。PaHub・目・ToF。会話は当面 Xiaozhi（差し替えやすく）。本番は ESP-IDF。
alwaysApply: true
---

# Obake（v0.5.1）

脳は **Stack-chan CoreS3**。カメラ・マイク・スピーカもそれ。旧案（専用 CoreS3・Unit LCD・ToF4M）に戻さない。

本番ファームは **ESP-IDF（Stack-chan と同じ手順）**。`arduino/obake_pahub_bringup` はハード確認のみ。書く前は Flash バックアップ。

CUSTOM セッションで **口・左右の目・ToF 距離** を必須表示（`stackchan/custom/obake/`）。通常 AI.AGENT は従来どおり。

## ハード

Port.A → PaHub v2.1（0x70）: CH0 左目 / CH1 右目 / CH2 ToF U010。目は Grove–Dupont。縦 OLED・余白白。ToF 上限 200 cm。

## ソフト方針

- 会話: 当面 Xiaozhi。あとで独自に差し替えるので境界を残す
- 呼びかけ: 「おばけちゃん」（`obake_wake_config.h`）。ウェイク LED は Stack-chan のまま
- CoreS3 液晶: **口**（CUSTOM）
- 移動: 別マイコン。CoreS3 は「来て」「20 cm 維持」を送る。通信は未定（Wi-Fi or BLE）。スタブを先に置く
- サーボ（足・首）: **当面完全停止**（`kForceAllServosOff`。本番に載せるときまで）
- 吹き出し: CUSTOM では出さない。下スワイプホームは従来どおり
- ウェイク: Multinet「おばけちゃん」
- assets: Multinet で ~5.5MB → `partitions.csv` の assets は **6MB**（不足するとランチャーアイコン全滅）

