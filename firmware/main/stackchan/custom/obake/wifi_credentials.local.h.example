/*
 * コピーして wifi_credentials.local.h にリネームし、SSID/パスワードを記入する。
 * wifi_credentials.local.h は .gitignore 済み（パスワードを commit/push しない）。
 */
#pragma once

#define OBAKE_WIFI_SSID "YOUR_SSID"
#define OBAKE_WIFI_PASSWORD "YOUR_PASSWORD"
